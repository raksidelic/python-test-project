stages:
  - test
  - report

variables:
  # Python'un bytecode (.pyc) oluşturmasını engeller
  PYTHONDONTWRITEBYTECODE: "1"
  
  # Screenshot her zaman açık
  TAKE_SCREENSHOT: "true"

  # --- VİDEO KAYIT AYARLARI (Dropdown Menü) ---
  # 'spec' yerine standart variables 'options' kullanıyoruz.
  RECORD_VIDEO:
    value: "on_failure"  # Varsayılan değer
    options:
      - "true"
      - "false"
      - "on_failure"
      - "on_success"
    description: "Video Kayıt Stratejisi"

run_tests:
  stage: test
  image: docker:29.1.2
  tags:
    - mac-m3-runner
  
  script:
    # 1. BAĞIMLILIKLARI YÜKLE
    - apk add --no-cache python3 py3-pip docker-compose

    # 2. ORTAM (.env) DOSYASINI HAZIRLA
    - echo "ARANGO_URL=http://arangoDB:8529" > .env
    - echo "ARANGO_DB_NAME=$ARANGO_DB_NAME" >> .env
    - echo "ARANGO_USER=$ARANGO_USER" >> .env
    - echo "ARANGO_PASSWORD=$ARANGO_PASSWORD" >> .env

    # AI
    - echo "AI_PROVIDER=$AI_PROVIDER" >> .env
    - echo "GEMINI_API_KEY=$GEMINI_API_KEY" >> .env
    - echo "OPENAI_API_KEY=$OPENAI_API_KEY" >> .env
    
    # Dropdown'dan seçilen değeri .env'ye yazıyoruz
    - echo "RECORD_VIDEO=$RECORD_VIDEO" >> .env
    
    - echo "Ortam dosyası oluşturuldu."
    
    # 3. AĞ AYARLARI
    - docker network create shared-network || true
    - docker network connect shared-network arangoDB || true
    
    # 4. KLASÖR İZİNLERİ
    - mkdir -p videos allure-results logs
    - chmod -R 777 videos allure-results logs
    
    # 5. AKILLI BAŞLATMA 🚀
    - python3 start_tests.py

  after_script:
    - BROWSERS_JSON=ignore_me docker-compose down --remove-orphans

  artifacts:
    when: always
    paths:
      - allure-results/
      - videos/
      - logs/
    expire_in: 3 days

generate_report:
  stage: report
  image: frankescobar/allure-docker-service
  tags:
    - mac-m3-runner
  script:
     - allure generate -c allure-results -o allure-report
  artifacts:
    paths:
      - allure-report/
    expire_in: 1 week
  needs:
    - run_tests
  when: always